#include <stdio.h>

void main()
{
    int dec;
    printf("Enter the number in decimal:");
    scanf("%d", &dec);
    printf("Octal Equivalent=%o\n", dec);
    printf("Hexadecimal Equivalent=%X", dec);
}